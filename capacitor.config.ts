import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.teconnect.app',
  appName: 'Te-connect',
  webDir: 'dist',
  bundledWebRuntime: false,
  loggingBehavior: 'production',
};

export default config;
